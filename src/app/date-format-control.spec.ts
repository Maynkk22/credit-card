import { DateFormatControl } from './date-format-control';

describe('DateFormatControl', () => {
  it('should create an instance', () => {
    expect(new DateFormatControl()).toBeTruthy();
  });
});
